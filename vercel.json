{
  "rewrites": [
    {
      "source": "/api/rss/:match*",
      "destination": "${process.env.RSS_API_HOST}/:match*"
    },
    {
      "source": "/api/memos/:match*",
      "destination": "${process.env.MEMOS_API_HOST}/api/:match*"
    }
  ]
}