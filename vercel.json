{
  "rewrites": [
    {
      "source": "/api/rss/:match*",
      "destination": "${RSS_API_HOST}/:match*"
    },
    {
      "source": "/api/memos/:match*",
      "destination": "${MEMOS_API_HOST}/api/:match*"
    }
  ]
}