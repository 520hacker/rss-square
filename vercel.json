{
  "rewrites": [
    {
      "source": "/api/rss/(.*)",
      "destination": "${RSS_API_HOST}/$1"
    },
    {
      "source": "/api/memos/(.*)",
      "destination": "${MEMOS_API_HOST}/api/$1"
    }
  ]
}